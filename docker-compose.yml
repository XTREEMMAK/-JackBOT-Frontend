services:
  jackbot-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: jackbot-frontend:local
    ports:
      - "3012:3000"
    env_file:
      - .env
    environment:
      PORT: 3000
    restart: unless-stopped
